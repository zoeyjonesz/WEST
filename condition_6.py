def main():
  recycle_pressure = "moderate"
  bta_pressure = "low"
  btb_pressure = "high"
  bta_valve = 50
  btb_valve = 50
  fully_open = 100
  compressor_speed = 100
  
  while recycle pressure == "moderate", bta_pressure == "low", btb_pressure == "high":
    if btb_valve != fully_open:
      btb_valve = fully_open

if __name__ =="__main__":
  main()
